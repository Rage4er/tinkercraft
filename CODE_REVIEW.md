# 🔍 Код-ревью: TinkerCraft Web

**Дата создания:** 2025-07-15
**Дата релиза:** 2026-08-19
**Ревьюер:** Koda AI
**Версия проекта:** v1.0.0
**Стек:** React 18 + TypeScript 5.7 + Three.js r170 + Zustand 5 + manifold-3d (WASM) + Vite 6 + pnpm

---

## 🎉 РЕЛИЗ v1.0.0

**Статус:** ✅ Все проблемы закрыты, проект готов к деплою.

### Сводка

| Метрика | Значение |
|---------|----------|
| Всего выявлено проблем | ~309 |
| Исправлено | ~196 |
| Архитектурные / НЕ БАГ | ~113 |
| **Активных проблем** | **0** |

### Полная история

Все исправленные проблемы задокументированы в [`CODE_REVIEW_ARCHIVE.md`](CODE_REVIEW_ARCHIVE.md).

Детальный журнал изменений — в [`CHANGELOG.md`](CHANGELOG.md).

---

## 🔍 Ревью цепочки загрузки Yandex SDK (2026-09-07)

**Симптом:** SDK загружается, стартовая реклама и баннер отображаются, но игра не запускается.

| # | Приоритет | Проблема | Статус |
|---|-----------|----------|--------|
| 1 | 🔴 КРИТИЧНО | SDK-dev-proxy во всех тест-скриптах указывал на порт **5173**, а Vite слушает **5000** (`vite.config.ts`). Прокси показывает свой SDK-мок (реклама + баннер), но iframe игры грузится с мёртвого порта → игра не стартует | ✅ ИСПРАВЛЕНО (`package.json`, `start-dev.bat`, `start-prod.sh`) |
| 2 | 🔴 КРИТИЧНО | `main.tsx` блокирует рендер React на `await initSdk()`, а `YaGames.init()` не имел таймаута — зависший init (postMessage/timing в iframe) = игра никогда не стартует | ✅ ИСПРАВЛЕНО — таймаут 10с через `Promise.race` (`platform/sdk.ts`) |
| 3 | 🟡 СРЕДНЕ | `GameplayAPI.start()` не вызывался при старте: effect модалок срабатывает раньше, чем `initPlatform()` резолвится (`getPlatform()` === null) | ✅ ИСПРАВЛЕНО — вызов после `initPlatform()` в bootstrap-effect (`App.tsx`) |
| 4 | 🟡 СРЕДНЕ | Cleanup `clearInterval(syncInterval)` возвращался из async `bootstrap()` и терялся — интервал syncToCloud не очищался при unmount (утечка, дубль в StrictMode) | ✅ ИСПРАВЛЕНО (`App.tsx`) |

### Рекомендации из ревью — все реализованы (2026-09-07)

| # | Приоритет | Рекомендация | Статус |
|---|-----------|--------------|--------|
| R1 | 🟡 СРЕДНЕ | Рендер не должен блокироваться SDK: `main.tsx` ждал `await initSdk()` до `createRoot()` (худший случай — чёрный экран 15с) | ✅ ИСПРАВЛЕНО — i18n по языку браузера → рендер сразу → SDK параллельно; язык из SDK через `applySdkLanguage()`/`i18n.changeLanguage()` (`main.tsx`, `i18n/init.ts`) |
| R2 | 🟡 СРЕДНЕ | `EconomyOnboarding` вызывал `platform.loadData()` на монтировании вне RAF — postMessage-гонка (паттерн error #185) | ✅ ИСПРАВЛЕНО — ждёт `initPlatform()` (идемпотентен) перед `loadData()`, флаг `cancelled` при unmount (`components/EconomyOnboarding.tsx`) |
| R3 | 🟡 СРЕДНЕ | `LoadingAPI.ready()` вызывался до готовности CSG-воркера — противоречие чек-листу A.1 («нет экранов загрузки» в момент Game Ready) | ✅ ИСПРАВЛЕНО — новый `IPlatform.loadingReady()`, вызов из App.tsx при `workerOk`, fallback-таймер 15с в `yandex.ts` (`platform/types.ts`, `platform/yandex.ts`, `platform/clean.ts`, `App.tsx`) |
| R4 | 🟡 СРЕДНЕ | `getPlayer()` в `yandex.ts init()` без таймаута — зависший getPlayer блокировал init() и загрузку экономики | ✅ ИСПРАВЛЕНО — `Promise.race` с таймаутом 5с → guest mode (`platform/yandex.ts`) |

### Текущий порядок загрузки (после исправлений)

```
1. i18n (язык браузера, мгновенно)          — main.tsx
2. React render (НЕ ждёт SDK)               — main.tsx
3. initSdk() параллельно (таймаут 10с)      — platform/sdk.ts
   ├─ applySdkLanguage() — язык из SDK      — i18n/init.ts
   └─ initPlatform() → RAF: banner, getPlayer (таймаут 5с) — platform/yandex.ts
4. App.tsx bootstrap: initPlatform() → GameplayAPI.start() → economy load
5. CSG-воркер готов (workerOk) → LoadingAPI.ready() (fallback 15с)
```

---

## 🔍 Ревью внедрения экономики ECONOMY.md v2.0 (2026-09-07)

**Симптом проверки:** все ли шаги §1–§7 выполнены верно; все ли реварды доступны в UI; все ли бейджи расставлены.

### Сводка по разделам

| Раздел | Статус | Примечание |
|--------|--------|------------|
| §2 Заработок (бонус 50, реклама 50×3, квесты 20/30/50, использование +1×30, кэшбэк V2 1–25×3) | ✅ | `economy-config.ts` значения совпадают с §2; максимум 405/день сходится |
| §2.1 Кэшбэк V2 (формула коэффициентов, кап 25) | ✅ | `calculateCashbackV2` = min(25, 1+масштаб+фигуры+инструменты+категории); разбивка видна в ExportModal до оплаты (§2.1 UI) |
| §3.1 Экспорт 50/1📺, импорт 100/2📺 | ✅ | ExportModal/ImportModal; импорт: 2 рекламы подряд, блок если `todayAdsWatched+2>3` |
| §3.2 Аренда 24ч: текст 75, палитра 75, баннер 50/1📺 | ✅ | отсчёт от момента покупки (serverTime+24h) |
| §3.3 Подписки 700/7д, 2000/30д, без автосписания | ✅ | состав: безлимит эксп/имп (ImportModal bypass), текст, палитра, баннер off |
| §4 Квесты V2: пул 15, 5 категорий, 3/день (1🌟+1⭐+1🏆), зачёт в save/export | ✅ | `commitQuests()` при saveDoodle/exportStl; событийные export/import/export_stl_large по триггерам |
| §5 Анти-фарм: серверное время, expiry-метки | ✅ | все сбросы/кулдауны через `getServerTime()` (кэш 30с); аренды/подписки — абсолютные timestamp |
| §6.1 HUD | ⚠️ | `EconomyHUD.tsx` реализован, но **не смонтирован** (удалён из App при интеграции в PropertiesPanel §6.2) — токены/бонус/реклама доступны в панели. Дубль HUD+панель не требуется по §6.2 |
| §6.2 Панель (PropertiesPanel, ничего не выделено) | ✅ | токены+бонус+реклама+кулдаун+лимит, квесты 3 карточки с прогрессом, аренды, подписки, баннер off |
| §6.3 Кнопка баннера — 3 места | ✅ | EconomyBanner (на баннере), EconomyPanel (панель), EconomyShop (магазин); состояния оффер→активна→оффер |
| §6.4 Бейджи | ✅/⚠️ | карта: Экспорт 50/1📺, Импорт 100/2📺 (Toolbar Badge), Текст 75 (LeftPanel), аренды/подписки/баннер-off 💰/📺 (EconomyPanel/Shop/Banner). См. проблемы E1–E2 |
| §6.5 Модалки оплаты | ✅/⚠️ | disabled-состояния с причинами («не хватает N», «лимит дня»); разбивка кэшбэка в экспорте. См. E3 |
| §6.6 i18n EN/RU | ✅ | 382/381 ключ, все economy.* ключи на месте; расхождение только в плюрализации projectManager (не экономика) |
| §6.7 Онбординг (3 шага, флаг в облако) | ✅ | EconomyOnboarding: HUD→бейджи→квесты, `onboardingDone` в облако |
| §7 Хранение | ✅ | экономика в облако (`setData`), проекты IndexedDB, UI локально; clean-версия: платформа stub, EconomyPanel/Shop/Banner не рендерятся (`getPlatform()===null`) |

### Найденные проблемы

| # | Приоритет | Проблема | Статус |
|---|-----------|----------|--------|
| E1 | 🟡 СРЕДНЕ | **Бейдж Текст в LeftPanel не учитывал аренду `text3d`** — только подписку; при активной аренде бейдж оставался | ✅ ИСПРАВЛЕНО — `textActive = hasActiveSubscription() \|\| rentals.text3d активна` (`LeftPanel.tsx`) |
| E2 | 🟡 СРЕДНЕ | **`exportLocked`/`importLocked`/`textLocked` вычислялись, но не использовались** (мёртвый код); бейджи Toolbar читались через `getState()` (нерективно) | ✅ ИСПРАВЛЕНО — мёртвые переменные удалены; бейджи Toolbar переведены на реактивные селекторы (`Toolbar.tsx`) |
| E3 | 🟡 СРЕДНЕ | **ImportModal вызывал `onImport()` в render-фазе** при активной подписке — side-effect в теле компонента, двойной вызов в StrictMode | ✅ ИСПРАВЛЕНО — bypass подписки перенесён в `useEffect` с ref-защитой (`ImportModal.tsx`) |
| E4 | 🟢 НИЗКО | **`watchAdForBanner` не соблюдал кулдаун 5 мин** между rewarded-просмотрами | ✅ ИСПРАВЛЕНО — общий кулдаун через `isCooldownPassed(lastAdTimestamp)`, баннер-реклама тоже открывает паузу (`economy-store.ts`) |
| E5 | 🟢 НИЗКО | **Мёртвый код: `EconomyHUD` не смонтирован; emoji-бейджи инлайн-спанами** | ✅ ИСПРАВЛЕНО — `EconomyHUD.tsx` удалён; все инлайн emoji-бейджи (💎/📺) заменены на SVG-компонент `Badge` (PropertiesPanel ×4, EconomyShop ×4, EconomyBanner ×2). Эмодзи в UI отсутствуют — только SVG |
| E6 | 🟢 НИЗКО | **`lastExportHash` мутировался напрямую** вне `set()` — не попадал в persist/cloud, после F5 кэшбэк за ту же модель возможен повторно | ✅ ИСПРАВЛЕНО — новый action `setExportHash()`; поле включено в `partialize`, `syncToCloud`, `loadFromCloud` (`economy-store.ts`, `document-store.ts`) |
| E7 | 🟢 НИЗКО | **`initDailyQuests` сбрасывал день по `isDayPassed(lastDailyBonus)`** — у игрока, не берущего бонус, квесты пересоздавались при каждом запуске (прогресс терялся) | ✅ ИСПРАВЛЕНО — новое поле `lastQuestResetDate` (persist + облако), сброс только при смене суток с последнего сброса квестов (`economy-store.ts`) |

### Вердикт

**Внедрение экономики соответствует ECONOMY.md v2.0.** Все реварды доступны в UI (бонус/реклама/квесты/использование/кэшбэк), бейджи расставлены по карте §6.4 (экспорт/импорт/текст/аренды/подписки/баннер-off), модалки оплаты с disabled-причинами и разбивкой кэшбэка. Все 7 проблем E1–E7 исправлены (2026-09-07); эмодзи в UI полностью заменены на SVG-иконки.

> ⚠️ **Дополнено 2026-09-07:** последующее глубокое ревью выявило 18 новых проблем EC1–EC18 (5 P0), включая случаи, не покрытые поверхностной проверкой выше (см. секцию «Повторное ревью экономики»). Вердикт этого ревью относится только к статической сверке тарифов/лимитов/UI-карты.

---

## 🔍 Повторное ревью экономики ECONOMY.md v2.0 (2026-09-07, глубокое)

**Контекст:** глубокое ревью после закрытия E1–E7 — сверка фактического поведения с §2–§7 ECONOMY.md v2.0 по цепочкам выполнения (store → UI → модалки → document-store).

**Проверка сборки:** `pnpm typecheck` — 0 ошибок; `pnpm test` — 236/236 проходят. **Тестов на экономику нет** (`economy-store.test.ts` / `economy-config.test.ts` отсутствуют) — см. EC18.

### Сводка

| Приоритет | Кол-во | Проблемы |
|-----------|--------|----------|
| 🔴 P0 (критично) | 5 | EC1–EC5 |
| 🟡 P1 (средне) | 8 | EC6–EC13 |
| 🟢 P2 (низко) | 5 | EC14–EC18 |

### 🔴 P0 — критические (финансовые потери игрока / сломанные флоу)

| # | Приоритет | Проблема | Статус |
|---|-----------|----------|--------|
| EC1 | 🔴 P0 | **Баннер-оффер никогда не показывается (§6.3, место №1).** `bannerVisible` инициализируется `false` (`economy-store.ts:268`), и **нигде нет `setBannerVisible(true)`**. `EconomyBanner` первым делом делает `if (!bannerVisible) return null` — компонент мёртв. Из трёх мест §6.3 работают только 2 (панель, магазин) | ✅ ИСПРАВЛЕНО — инициализация `bannerVisible = true` в App.tsx bootstrap если нет подписки и аренда не активна (`App.tsx`) |
| EC2 | 🔴 P0 | **Импорт за 2 рекламы невозможен (§3.1).** `ImportModal.handleWatchAd` вызывает `watchAdForTokens()` дважды подряд, но после первого просмотра `lastAdTimestamp = now`, и второй вызов падает по кулдауну 5 мин (`economy-store.ts`). Итог: игрок посмотрел рекламу, получил +50 TC, но импорт **не выполнился** — токены начислены, флоу сломан | ✅ ИСПРАВЛЕНО — новая функция `watchAdsForImport(count)` в store — N реклам подряд без кулдауна между ними; ImportModal использует её (`economy-store.ts`, `ImportModal.tsx`) |
| EC3 | 🔴 P0 | **Экспорт платный для подписчиков (§3.3).** `ImportModal` имеет bypass по подписке (useEffect), а `ExportModal` — **нет**: подписчик с «безлимитным экспортом» платит 50 TC или смотрит рекламу. При этом бейджи Toolbar при подписке скрываются (`exportActive`) — UI противоречит поведению | ✅ ИСПРАВЛЕНО — bypass подписки в ExportModal через useEffect с ref-защитой (`ExportModal.tsx`) |
| EC4 | 🔴 P0 | **Квест `export_stl` (hard, target 10) засчитывается от любого экспорта.** `completeEventQuest('export_stl')` (`document-store.ts:1065`) помечает **все** квесты с этим триггером как выполненные без проверки `target`. Плюс вызов идёт **после** `commitQuests()` — награда уходит только при следующем save/export | ✅ ИСПРАВЛЕНО — `completeEventQuest` принимает `objectCount`, проверяет target; порядок вызовов в `exportStl`: триггеры ДО `commitQuests` (`economy-store.ts`, `document-store.ts`) |
| EC5 | 🔴 P0 | **Превью кэшбэка в ExportModal ≠ фактическое начисление (§2.1).** Модалка дублирует формулу вручную вместо `calculateCashbackV2` и расходится с ней: ① toolDiv: модалка `[0,0,2,4,6]`, конфиг — `Math.min(categories, 6)` (даёт 1/2/3/4); ② objectCount: модалка исключает `import_mesh`/`text3d`, `scanForCashback` — нет; ③ дневной лимит 3 начисления не учтён: при исчерпанном лимите игрок заплатит `netCost` (уменьшенный на кэшбэк), но `calculateAndClaimCashback` вернёт 0 — платит больше обещанного | ✅ ИСПРАВЛЕНО — `calculateCashbackBreakdown` в `economy-config.ts` — единая функция для ExportModal и store; ExportModal использует общий `scanForCashback` и config; учёт дневного лимита 3/день (`economy-config.ts`, `ExportModal.tsx`) |

### 🟡 P1 — средние (отклонения от §6, UX/архитектура)

| # | Приоритет | Проблема | Статус |
|---|-----------|----------|--------|
| EC6 | 🟡 P1 | **HUD §6.1 не показывается при выделенном объекте.** `EconomyHUD` (токены, бонус, реклама) смонтирован в App.tsx только когда `!firstSelected` — при выделении объекта HUD исчезает, пользователь не видит баланс | ✅ ИСПРАВЛЕНО — новый компонент `EconomyMiniHUD` в PropertiesPanel при выделенном объекте; HUD баланса виден всегда (`EconomyMiniHUD.tsx`, `PropertiesPanel.tsx`) |
| EC7 | 🟡 P1 | **Бейджи ⏱/👑 не расставлены кроме экспорта/импорта.** В LeftPanel бейдж текста скрывается только при подписке, не при аренде; в PropertiesPanel бейдж палитры всегда виден; в EconomyShop бейджи аренды/подписок статичны | ✅ ИСПРАВЛЕНО — `isActive` передан в все бейджи: LeftPanel (textActive), PropertiesPanel (canUseExtendedPicker), EconomyShop (hasRental/hasActiveSub), EconomyBanner (`LeftPanel.tsx`, `PropertiesPanel.tsx`, `EconomyShop.tsx`, `EconomyBanner.tsx`) |
| EC8 | 🟡 P1 | **Zustand-антипаттерн в LeftPanel.** Составной селектор `{ tokens, textActive }` пересоздаётся при каждом рендере; `textActive` вычисляется как объект с побочным эффектом `hasActiveSubscription()` | ✅ ИСПРАВЛЕНО — разбивка на два примитивных селектора; добавлены read-only хелперы `hasActiveSubscriptionRO` и `hasRentalRO` (`LeftPanel.tsx`, `economy-store.ts`) |
| EC9 | 🟡 P1 | **«Купить» ведёт на вкладку `objects` вместо `shop`.** Кнопка «купить за 75» в PropertiesPanel вызывает `setActiveTab('objects')` — пользователь видит список фигур, а не магазин | ✅ ИСПРАВЛЕНО — `setActiveTab('shop')` в PropertiesPanel и App.tsx (`PropertiesPanel.tsx`, `App.tsx`) |
| EC10 | 🟡 P1 | **`commitQuests` платит 1 раз/день (ломает прогрессивный зачёт §4).** `lastQuestCommitDate` блокирует повторный коммит в тот же день — игрок делает 5 экспортов, получает награду только с первого | ✅ ИСПРАВЛЕНО — убрана защита `lastQuestCommitDate`, квесты коммитятся при каждом save/export (`economy-store.ts`) |
| EC11 | 🟡 P1 | **`hasRental`/`hasActiveSubscription` мутат state в render-фазе.** При каждом рендере вызывается `set()` для очистки истёкшей аренды/подписки — потенциально дублированные апдейты | ✅ ИСПРАВЛЕНО — read-only версии `hasActiveSubscriptionRO`/`hasRentalRO` без `set()` для селекторов; mutating-версии сохранены для явного вызова (`economy-store.ts`, `PropertiesPanel.tsx`, `Toolbar.tsx`, `EconomyShop.tsx`, `EconomyBanner.tsx`) |
| EC12 | 🟡 P1 | **Нет тоста о кэшбэке (§2.1).** Пользователь не видит подтверждение начисления кэшбэка после экспорта — только результат в экспорте | ✅ ИСПРАВЛЕНО — `notify` в document-store при positive cashback (`document-store.ts`) |
| EC13 | 🟡 P1 | **Нет подписи «рекламой не открывается» (§6.5).** Для текст/палитра: только [💎 N] — нет указания что реклама не подходит | ✅ ИСПРАВЛЕНО — i18n-ключ `economy.adNotRentable`, добавлен в PropertiesPanel (locked-секция палитры) и тост в handleAddText (`translation.json`, `PropertiesPanel.tsx`, `App.tsx`) |
| EC6 | 🟡 P1 | **HUD §6.1 («всегда видимый») отсутствует.** Экономика интегрирована в PropertiesPanel и видна **только когда ничего не выделено** (PropertiesPanel.tsx:509–514). Выделение объекта скрывает баланс/бонус/рекламу/квесты. Комментарий App.tsx:20 ссылается на §6.2, но §6.1 требует постоянный HUD — либо вернуть HUD, либо зафиксировать отклонение в ECONOMY.md | 🔲 НЕ ИСПРАВЛЕНО |
| EC7 | 🟡 P1 | **Бейджи ⏱/👑 «когда активен» (§6.4) не расставлены.** Реализован только режим «нет доступа» (💰/📺). `Badge.tsx` умеет `isActive` (корона/часы сверху), но флаг передаётся **только для экспорта/импорта в Toolbar** (`isActive={exportActive}`). Для text3d (`LeftPanel`), палитры (`PropertiesPanel`), аренд/подписок/баннера (`EconomyShop`, `EconomyBanner`) бейдж при активном доступе просто **исчезает** вместо показа ⏱/👑 — игрок не видит, что доступ арендован и сколько осталось. `IconBadge.tsx` (timerBadge/proBadge) — мёртвый код, не используется нигде | 🔲 НЕ ИСПРАВЛЕНО |
| EC8 | 🟡 P1 | **Zustand-антипаттерн в LeftPanel:69** — `useEconomyStore(s => ({ tokens, textActive }))` возвращает новый объект на каждый вызов без `useShallow` → в Zustand v5 бесконечные ре-рендеры / warning `getSnapshot should be cached` | 🔲 НЕ ИСПРАВЛЕНО |
| EC9 | 🟡 P1 | **«Купить» ведёт не в магазин.** PropertiesPanel.tsx:620,632 и App.tsx:677 делают `setActiveTab('objects')` вместо `'shop'` (при том что комментарий говорит «открыть магазин») — пользователь не попадает на точку покупки | 🔲 НЕ ИСПРАВЛЕНО |
| EC10 | 🟡 P1 | **`commitQuests` — одна выплата в день (Y3.15).** Защита `lastQuestCommitDate` блокирует повторный коммит в течение 24ч — квесты, выполненные **после** первого save/export, не будут оплачены до следующего дня. Прогрессивный зачёт из §4 («прогресс живой, зачёт в saveProject()/exportStl()») сломан | 🔲 НЕ ИСПРАВЛЕНО |
| EC11 | 🟡 P1 | **Побочные эффекты в render-фазе.** `hasRental()` / `hasActiveSubscription()` вызывают `set()` при истечении и дёргаются прямо в render/selector (EconomyPanel:77–78, EconomyShop:41,99, Toolbar:118–119, EconomyBanner:16–17) — нарушение конвенции «actions вне render» | 🔲 НЕ ИСПРАВЛЕНО |
| EC12 | 🟡 P1 | **Нет тоста о кэшбэке после экспорта (§2.1 UI: «и в тосте после»)** — только `console.log` в `calculateAndClaimCashback` (`economy-store.ts:424`). `notify()` из `store/notifications.ts` не используется | 🔲 НЕ ИСПРАВЛЕНО |
| EC13 | 🟡 P1 | **Нет подписи «рекламой не открывается» для текста/палитры (§6.5)** — в TextModal и locked-секции палитры PropertiesPanel подпись отсутствует (в i18n нет соответствующих ключей) | 🔲 НЕ ИСПРАВЛЕНО |

### 🟢 P2 — низкие (техдолг, производительность, документация)

| # | Приоритет | Проблема | Статус |
|---|-----------|----------|--------|
| EC14 | 🟢 P2 | **`server-time.ts`: кэш пишется, но не читается** — `getServerTime()` игнорирует `getCachedServerTime()` (используется только `resetServerTimeCache` в тестах). При этом EconomyPanel дёргает SDK **дважды в секунду** (таймер 1s → `getAdCooldownRemaining()` + `checkBonusAvailable()`, PropertiesPanel.tsx:85–97) | 🔲 НЕ ИСПРАВЛЕНО |
| EC15 | 🟢 P2 | **`loadFromCloud` не восстанавливает `lastQuestCommitDate`** — защита Y3.15 от двойного коммита ослаблена после перезагрузки страницы (поле есть в `partialize`, но отсутствует в `loadFromCloud`) | 🔲 НЕ ИСПРАВЛЕНО |
| EC16 | 🟢 P2 | **Мёртвый код:** `QuestPanel.tsx` (не импортируется нигде), `IconBadge.tsx` (props timerBadge/proBadge не используются), `ECONOMY_UI.showTokensInStatusBar` (в StatusBar токенов нет), `TRIGGER_LABELS` в `economy-ui-config.ts` (квесты берут подписи через i18n напрямую), `calculateCashback()` deprecated | 🔲 НЕ ИСПРАВЛЕНО |
| EC17 | 🟢 P2 | **Кнопки бонуса/рекламы исчезают вместо состояний §6.1** («получено · до 00:00» / «лимит дня») — при недоступности рендерится пустое место (`bonusSection`, PropertiesPanel.tsx:148–167) | 🔲 НЕ ИСПРАВЛЕНО |
| EC18 | 🟢 P2 | **Нет тестов на экономику** — отсутствуют `economy-config.test.ts` (формула `calculateCashbackV2`, `scanForCashback`, примеры §2.1: 5 кубов=2, стул=8, дом=19, мега=25) и `economy-store.test.ts` (commitQuests, лимиты, кулдауны). AGENTS.md требует тесты для store-изменений | 🔲 НЕ ИСПРАВЛЕНО |

### Дополнительно (документация)

- `AGENTS.md` ссылается на `EconomyHUD.tsx` — файл не существует (удалён в E5). Таблицу «Ключевые файлы» нужно обновить.

### Ответы на вопросы ревью

**Все ли шаги выполнены верно?** Ядро реализовано корректно: тарифы §3 совпадают, серверное время §5 применяется во всех сбросах/кулдаунах/арендах, persist+облако §7 работают, квесты V2 §4 оцениваются по состоянию проекта (14 точек вызова в document-store), онбординг §6.7 и clean-режим работают. Но EC1–EC5 делают невыполнимыми: показ баннера-оффера, импорт за рекламу, бесплатный экспорт по подписке и честный расчёт кэшбэка.

**Все ли реварды доступны в UI?**

| Ревард | Статус |
|---|---|
| Ежедневный бонус +50 | ✅ панель (но нет состояния «до 00:00» — EC17) |
| Реклама +50 (≤3/день, кд 5 мин) | ✅ панель, таймер + лимит |
| Квесты +20/+30/+50 | ✅ панель, живой прогресс (но выплата 1/день — EC10) |
| Использование +1 (≤30/день) | ✅ (без UI-индикации — допустимо) |
| Кэшбэк V2 +1…25 | ⚠️ превью есть, но формула расходится с фактом (EC5), тоста нет (EC12) |
| Подписки 700/2000 | ✅ панель + магазин |

**Все ли бейджи расставлены?** Карта «нет доступа» §6.4 выполнена полностью: Экспорт 50/1📺 ✅, Импорт 100/2📺 ✅ (Toolbar), Текст 75 ✅ (LeftPanel), Палитра 75 ✅ (PropertiesPanel), Баннер off 50/1📺 ✅ (баннер/панель/магазин). Аренды/подписки в панели и магазине — 💰 ✅. Но активные состояния ⏱/👑 не расставлены, кроме экспорта/импорта (EC7). `pointer-events:none` и позиционирование соблюдены.

### 🟢 P2 — низкие (оптимизация, мёртвый код)

| # | Приоритет | Проблема | Статус |
|---|-----------|----------|--------|
| EC14 | 🟢 P2 | **Кэш `getServerTime` не читается** при опросе SDK 2×/сек — кэш только записывается, но не проверяется перед fetch | ✅ ИСПРАВЛЕНО — проверка кэша перед fetch в `getServerTime` (`server-time.ts`) |
| EC15 | 🟢 P2 | **`loadFromCloud` не восстанавливает `lastQuestCommitDate`** (удалён за мёртвость) | ✅ ИСПРАВЛЕНО — `lastQuestCommitDate` удалён из store (см. EC10) |
| EC16 | 🟢 P2 | **Мёртвый код:** `QuestPanel` (интегрирован в PropertiesPanel), `TRIGGER_LABELS`, `showTokensInStatusBar`, `QUEST_LABELS` не импортируются нигде | ✅ ИСПРАВЛЕНО — `QuestPanel.tsx` заменён на заглушку; `TRIGGER_LABELS`/`showTokensInStatusBar`/`QUEST_LABELS` удалены из `economy-ui-config.ts` (`QuestPanel.tsx`, `economy-ui-config.ts`) |
| EC17 | 🟢 P2 | **Кнопки бонуса/рекламы исчезают вместо состояния «до 00:00»/«лимит дня».** По §6.1 кнопки должны показывать время/лимит, а не скрываться | ✅ ИСПРАВЛЕНО — бонус показывает «Уже получено» с иконкой часов; реклама показывает кулдаун `M:SS` или «Лимит рекламы» (`PropertiesPanel.tsx`, `EconomyMiniHUD.tsx`) |
| EC18 | 🟢 P2 | **Нет тестов на экономику.** `economy-store.test.ts` / `economy-config.test.ts` отсутствуют — кэшбэк, квесты, кулдауны не покрыты | ✅ ИСПРАВЛЕНО — `economy-config.test.ts` (21 тест): `calculateCashbackV2` (11 тестов), `calculateCashbackBreakdown` (6 тестов), `scanForCashback` (4 теста) (`economy-config.test.ts`) |

### Рекомендуемый порядок исправления

1. **EC2, EC5** — прямые финансовые потери игрока (переплата/застрявшие токены). ✅ ЗАКРЫТО
2. **EC1, EC3, EC4** — сломанные флоу (баннер, подписка, квест hard). ✅ ЗАКРЫТО
3. **EC7, EC9** — видимые UX-отклонения от §6.4/§6.5. ✅ ЗАКРЫТО
4. **EC17, EC18** — UX состояний и тесты. ✅ ЗАКРЫТО

---

## 🔍 Ревью экономики ECONOMY.md (2026-09-10)

**Контекст:** глубокое ревью спецификации [`ECONOMY.md`](ECONOMY.md) против фактической реализации экономики ([`economy-store.ts`](web-app/src/store/economy-store.ts), [`economy-config.ts`](web-app/src/store/economy-config.ts), [`economy-ui-config.ts`](web-app/src/store/economy-ui-config.ts), компоненты `Economy*`/`QuestPanel`, [`platform/*`](web-app/src/platform/index.ts), точки интеграции `App`/`Toolbar`/`PropertiesPanel`/`LeftPanel`).

**Полный отчёт:** [`docs/ECONOMY_CODE_REVIEW.md`](docs/ECONOMY_CODE_REVIEW.md)

**Статус:** 🔴 ОТКРЫТ — ожидает исправлений. **Экономика не готова к релизу без закрытия всех P0.**

### Сводка

| Приоритет | Кол-во | Проблемы |
|-----------|--------|----------|
| 🔴 P0 (критично) | 6 | P0-1 … P0-6 |
| 🟡 P1 (средне) | 9 | P1-1 … P1-9 |
| 🟢 P2 (низко) | 7 | P2-1 … P2-7 |

### Связь с предыдущими ревью

- **E1–E7 (2026-09-07)** — закрыты, но новое ревью выявило неполные исправления: **E6** (lastExportHash) закрыт, однако анти-фарм кэшбэка всё равно не работает → **P0-1**; **E7** (lastQuestResetDate) закрыт, но `todayCashbacks` сбрасывается только при старте → **P1-1**
- **EC1–EC18 (2026-09-07)** — закрыты, но: **EC8/EC11** (read-only селекторы) закрыты, а `LeftPanel.tsx:69` всё ещё вызывает мутирующий `hasActiveSubscription()` в render → **P1-4**; **EC16** (мёртвый код) закрыт, но `calculateCashback` v1 `@deprecated` не удалён → **P2-7**

### Статус исправлений

| # | Приоритет | Проблема | Статус |
|---|-----------|----------|--------|
| P0-1 | 🔴 P0 | Анти-фарм кэшбэка не работает: хэш модели не проверяется, повторный экспорт той же модели даёт повторный кэшбэк (`economy-store.ts:1052`, `document-store.ts:1047`) | 🔲 ОТКРЫТО — ожидает исправления |
| P0-2 | 🔴 P0 | Expiry аренд/подписок и кулдауны по локальному `Date.now()` вместо серверного времени (§5) — перевод часов продлевает доступ (`economy-store.ts:478,488,518,529,537`) | 🔲 ОТКРЫТО — ожидает исправления |
| P0-3 | 🔴 P0 | `syncToCloud` теряет обновления при `pendingSync=true` и падениях — потерянные начисления (`economy-store.ts:789`) | 🔲 ОТКРЫТО — ожидает исправления |
| P0-4 | 🔴 P0 | `lastSavedData` не в `partialize` — облако может быть перезаписано старыми локальными данными (`economy-store.ts:841`) | 🔲 ОТКРЫТО — ожидает исправления |
| P0-5 | 🔴 P0 | Вся экономика client-side: правка localStorage = неограниченные токены/подписки; syncToCloud без валидации (`economy-store.ts:244`) | 🔲 ОТКРЫТО — ожидает исправления |
| P0-6 | 🔴 P0 | Clean-фолбэк SDK: при падении `yandex.init()` экономика остаётся активной, заработок невозможен (`platform/index.ts:10`, `platform/yandex.ts:29`) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-1 | 🟡 P1 | `todayCashbacks` сбрасывается только в `initDailyQuests()` — лимит 3/день не восстанавливается без перезагрузки (`economy-store.ts:456,638`) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-2 | 🟡 P1 | `objectCount` считается по-разному: экспорт исключает import_mesh/text3d, квесты считают все (`economy-store.ts:121`, `document-store.ts:1044`) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-3 | 🟡 P1 | Хэш кэшбэка не включает `operations` — изменение CSG-структуры даёт тот же хэш (`document-store.ts:1047`) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-4 | 🟡 P1 | `LeftPanel.tsx:69` использует мутирующий `hasActiveSubscription()` в render-фазе (связано с EC8/EC11) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-5 | 🟡 P1 | Проверка доступа к 3D-тексту дублируется в 4 местах с разной логикой (`App.tsx:441,678`, LeftPanel, Toolbar) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-6 | 🟡 P1 | `watchAdsForImport(2)`: отказ второй рекламы → первая показана, токены не начислены (`economy-store.ts:374`) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-7 | 🟡 P1 | `watchAdForBanner` не учитывает лимит 3/день и не тратит `todayAdsWatched` (`economy-store.ts:396`) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-8 | 🟡 P1 | `EconomyMiniHUD` считает кулдаун/бонус по `Date.now()` — рассинхрон с PropertiesPanel (`EconomyMiniHUD.tsx:27,35`) | 🔲 ОТКРЫТО — ожидает исправления |
| P1-9 | 🟡 P1 | `csg_complex` считается только для group с ids.length>=2, без subtract/intersect (`economy-store.ts:735`) | 🔲 ОТКРЫТО — ожидает исправления |
| P2-1 | 🟢 P2 | `isDayPassed` фолбэчит на `Date.now()` в clean-режиме (`economy-config.ts:108`) | 🔲 ОТКРЫТО — ожидает исправления |
| P2-2 | 🟢 P2 | `serverTime = lastAdTimestamp ?? Date.now()` (`economy-store.ts:373`) | 🔲 ОТКРЫТО — ожидает исправления |
| P2-3 | 🟢 P2 | Флаг онбординга сохраняется напрямую в `platform.saveData` минуя store (`EconomyOnboarding.tsx:86`) | 🔲 ОТКРЫТО — ожидает исправления |
| P2-4 | 🟢 P2 | Онбординг рендерится в clean-режиме (`App.tsx:565`) | 🔲 ОТКРЫТО — ожидает исправления |
| P2-5 | 🟢 P2 | Событийные квесты export_stl/import_stl показывают прогресс «0/1» после выполнения (`economy-store.ts:744`) | 🔲 ОТКРЫТО — ожидает исправления |
| P2-6 | 🟢 P2 | Кэшбэк начисляется синхронно до фактического успеха экспорта (`economy-store.ts:465`) | 🔲 ОТКРЫТО — ожидает исправления |
| P2-7 | 🟢 P2 | Устаревший `calculateCashback` v1 @deprecated не удалён (связано с EC16; `economy-config.ts:219`) | 🔲 ОТКРЫТО — ожидает исправления |

### Расхождения спецификации

| Пункт ECONOMY.md | Требование | Факт | Статус |
|------------------|------------|------|--------|
| `maxPerDay = 405` | декларирован максимум/день | не enforce | 🔲 ОТКРЫТО |
| `realisticPerDay = 240` | реалистичный доход | не используется | 🔲 ОТКРЫТО |
| IAP 10 TC = 1 ян (фаза B) | покупка в приложении | отсутствует в конфиге | 🔲 ОТКРЫТО |
| §5 Анти-фарм | хэш модели, защита кэшбэка | реализован частично (P0-1) | 🔲 ОТКРЫТО |
| §7 Clean-режим | полное отключение экономики | не отключает полностью (P0-6) | 🔲 ОТКРЫТО |

### Вердикт

Экономика **не готова к релизу**: 6 P0-проблем (анти-фарм, серверное время, потеря начислений в облаке, client-side доверие, clean-фолбэк) делают невозможной честную монетизацию. После закрытия P0 повторить ревью P1/P2 и обновить этот раздел. Полный отчёт и чек-лист закрытия — в [`docs/ECONOMY_CODE_REVIEW.md`](docs/ECONOMY_CODE_REVIEW.md).

---

## 📌 Будущие направления

Следующие направления для будущих итераций (не являются активными проблемами):

- **Параметрическая история операций (Фаза 8)** — редактирование параметров примитивов, CSG, mirror, fillet, extrude в Timeline
- **Импорт/экспорт профессиональных форматов** — STEP, IGES, 3MF
- **Коллаборативное редактирование** — CRDT / WebSocket
- **Физическая симуляция** — Rapier WASM

> Все планы см. в [`DEVELOPMENT_PLAN.md`](DEVELOPMENT_PLAN.md).
